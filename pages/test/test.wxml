<!-- 测试页 -->
<view class="container">
  <!-- 未测试状态 -->
  <view class="test-intro" wx:if="{{!currentTest}}">
    <image class="test-icon" src="/images/test-large.png" />
    <text class="title">AI能力测试</text>
    <text class="desc">通过AI评估，准确了解你的真实水平</text>

    <view class="feature-list">
      <view class="feature-item">
        <text class="feature-icon">✓</text>
        <text class="feature-text">10道精选题目</text>
      </view>
      <view class="feature-item">
        <text class="feature-icon">✓</text>
        <text class="feature-text">5个维度评估</text>
      </view>
      <view class="feature-item">
        <text class="feature-icon">✓</text>
        <text class="feature-text">AI智能分析</text>
      </view>
    </view>

    <button class="btn btn-primary start-btn" bindtap="startTest">开始测试</button>

    <!-- 测试历史 -->
    <view class="history-section" wx:if="{{testHistory.length > 0}}">
      <text class="section-title">测试历史</text>
      <view class="history-list">
        <view 
          class="history-item" 
          wx:for="{{testHistory}}" 
          wx:key="id"
          bindtap="viewResult"
          data-id="{{item.id}}"
        >
          <view class="history-info">
            <text class="history-level">Lv.{{item.level}}</text>
            <text class="history-date">{{item.date}}</text>
          </view>
          <text class="history-score">{{item.score}}分</text>
        </view>
      </view>
    </view>
  </view>

  <!-- 测试中状态 -->
  <view class="test-ongoing" wx:if="{{currentTest && !testResult}}">
    <view class="progress-bar">
      <view class="progress-fill" style="width: {{(currentIndex + 1) / questions.length * 100}}%"></view>
    </view>
    <text class="progress-text">{{currentIndex + 1}}/{{questions.length}}</text>

    <view class="question-card">
      <text class="question-text">{{currentQuestion.text}}</text>

      <view class="options-list">
        <view 
          class="option-item {{selectedOption === item.value ? 'selected' : ''}}"
          wx:for="{{currentQuestion.options}}"
          wx:key="value"
          bindtap="selectOption"
          data-value="{{item.value}}"
        >
          <text class="option-label">{{item.label}}</text>
          <text class="option-content">{{item.content}}</text>
        </view>
      </view>
    </view>

    <view class="action-bar">
      <button class="btn btn-secondary" bindtap="prevQuestion" wx:if="{{currentIndex > 0}}">上一题</button>
      <button class="btn btn-primary" bindtap="nextQuestion" disabled="{{!selectedOption}}">
        {{currentIndex === questions.length - 1 ? '提交' : '下一题'}}
      </button>
    </view>
  </view>

  <!-- 测试结果 -->
  <view class="test-result" wx:if="{{testResult}}">
    <view class="result-icon {{testResult.level >= 4 ? 'expert' : ''}}">
      <text class="level-badge">{{testResult.levelName}}</text>
    </view>
    <text class="result-title">测试完成</text>
    <text class="result-score">得分：{{testResult.score}}/100</text>

    <!-- 维度分析 -->
    <view class="result-analysis">
      <text class="analysis-title">五维能力分析</text>
      <view class="analysis-item" wx:for="{{testResult.analysis}}" wx:key="name">
        <text class="analysis-name">{{item.name}}</text>
        <view class="analysis-bar">
          <view class="analysis-fill {{item.score >= 40 ? 'high' : item.score >= 20 ? 'medium' : 'low'}}" style="width: {{item.score}}%"></view>
        </view>
        <text class="analysis-score">{{item.score}}分</text>
      </view>
    </view>

    <!-- 学习建议 -->
    <view class="suggestions-section" wx:if="{{testResult.suggestions.length > 0}}">
      <text class="suggestions-title">💡 学习建议</text>
      <view class="suggestion-item" wx:for="{{testResult.suggestions}}" wx:key="index">
        <text class="suggestion-dot">•</text>
        <text class="suggestion-text">{{item}}</text>
      </view>
    </view>

    <!-- 题目回顾 -->
    <view class="review-section">
      <text class="review-title">📖 题目回顾</text>
      <view class="review-item" wx:for="{{questions}}" wx:key="id">
        <view class="review-question">
          <text class="review-num">{{index + 1}}.</text>
          <text class="review-text">{{item.text}}</text>
        </view>
        <view class="review-answer">
          <text class="your-answer {{answers[index] === item.correctAnswer ? 'correct' : 'wrong'}}">
            你的答案：{{answers[index] || '未答'}}
          </text>
          <text class="correct-answer">正确答案：{{item.correctAnswer}}</text>
        </view>
        <view class="review-explanation" wx:if="{{item.explanation}}">
          <text class="explanation-text">{{item.explanation}}</text>
        </view>
      </view>
    </view>

    <view class="result-actions">
      <button class="btn btn-secondary" bindtap="retest">重新测试</button>
      <button class="btn btn-primary" bindtap="goToMatch">去匹配</button>
    </view>
  </view>
</view>
